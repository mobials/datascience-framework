APP_ENV="{{ env }}"

METRICS_RABBIT_HOST="{{ rabbitmq.host }}"
METRICS_RABBIT_VHOST="{{ rabbitmq.vhost }}"
METRICS_RABBIT_USERNAME="{{ rabbitmq.username }}"
METRICS_RABBIT_PASSWORD="{{ rabbitmq.password }}"
METRICS_RABBIT_PORT="5672"
METRICS_RABBIT_EXCHANGE="{{ rabbitmq.exchange }}"
QUEUE="{{ rabbitmq.queue }}"

DB_USERNAME="{{ rds_username }}"
DB_PASSWORD="{{ rds_password }}"
DB_NAME="{{ rds_db_name }}"
DB_HOST="{{ rds_host }}"
DB_PORT="{{ rds_port }}"

AUTH_USERNAME="{{ metrics_auth.username }}"
AUTH_PASSWORD="{{ metrics_auth.password }}"
